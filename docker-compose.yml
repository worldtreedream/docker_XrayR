version: '3'
services: 
  xrayr: 
    image: ghcr.io/xrayr-project/xrayr:latest
    container_name: HIM
    volumes:
      - ./config.yml:/etc/XrayR/config.yml # 映射配置文件夹
    restart: always
    network_mode: host
