version: '3'
services: 
  xrayr: 
    image: dreamworldtree/mrayr:latest
    container_name: dreamworldtree
    volumes:
      - ./config.yml:/etc/XrayR/config.yml # 映射配置文件夹
      - ./cert:/etc/XrayR/cert
    restart: always
    network_mode: host
